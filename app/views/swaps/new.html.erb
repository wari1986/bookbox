<div class="container bg-indigo-100">

  <div class="card-body font-bold text-center ">
  <h1>Please select the book you want to swap</h1>
  </div>

  <div class="card-body">
    <%= simple_form_for([@user_book_relationship, @swap]) do |f| %>
      <%= f.input :swapped_book_id, collection: @mybooks %>
      <%= f.submit "Send" %>
    <% end %>
  </div>

</div>
®
